package shy.teamcook.appffix.user.controller;

import shy.teamcook.appffix.user.entity.CompanyUserEntity;

import java.util.ArrayList;

public class UserController {

    /**
     * 기능 목록
     * 회원추가
     * 이메일 체크 @ 포함되어있나 체크
     * 주민번호 뒷자리 6개 *처리
     *
     * 기업회원, 개인회원 나누는 법
     * while ()
     * 1. 상수 선언
     * 1. 개인 = PERSON
     * 2. 기업 = ENTERPRISE
     * (print) 개인이면 1, 기업이면 2를 입력하세요.
     * 입력 > 1
     * switch (scanner 변수명) => case PERSON: 개인회원 클래스.등록함수(); break; UserEntity.
     * case ENTERPRISE: 기업회원 클래스.등록함수(); break;
     *
     * 회원번호 생성 메소드
     * 비밀번호 암호화 메소드
     * 가입날짜 리턴 메소드
     * 고객등급 getter => getUserGrade() { return userGrade; }
     */
}
